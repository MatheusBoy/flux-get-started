---
apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: alpine
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: alpine
  policy:
    semver:
      range: 1.0.1

